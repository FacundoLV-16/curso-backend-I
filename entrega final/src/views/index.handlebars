<html>
  <head>
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{title}}</h1>
    <ul>
      {{#each productos}}
        <li>{{this.title}} - ${{this.price}}</li>
      {{/each}}
    </ul>
  </body>
</html>